// as for now we are using only one method, but in the future it is possible
// to add more, as API documentation has a lot of methods
export enum REQUEST_METHOD {
  ARTIST_SEARCH = 'artist.search',
}
